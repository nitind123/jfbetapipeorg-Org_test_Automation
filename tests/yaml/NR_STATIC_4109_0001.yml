pipelines:
  - name: pipelines_NR_STATIC_4109_0001
    steps:
      - name: NR_STATIC_4109_0001_STEP1
        type: Bash
        execution:
          onExecute:
            - echo "executing step on u18 static non root node pool"
